{{ attach_library('badcamp/sponsors') }}
<div class="{{ classes|default('') }}">
  {% for item in items %}
    {%
      include '@badcamp/card/card.twig' with {
        'eyebrow': item.eyebrow,
        'icon': item.icon,
        'title': item.title,
        'body': item.body,
        'more': item.more,
        'moreText': item.moreText,
        'class': 'featured-sponsors__card',
      } only
    %}
  {% endfor %}
</div>
